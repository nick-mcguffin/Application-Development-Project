### Common ###
module:
  name: TEST

spring:
  application:
    view-attachment-path: "http://localhost:8080/api/v1/partners/view-attachment/"
    name: Work Integrated Learning Management Application - WILMA

  profiles:
    active: dev # Set this as either dev or prod

  mail:
    # Credentials for the gmail smtp service will need to be added to your test run configuration
    site-email: wilmaproject.dev@gmail.com
    host: smtp.gmail.com
    port: 587
    username: wilmaproject.dev@gmail.com
    password: bditlkfcuotzojpu
    properties:
      mail:
        smtp:
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

api:
  v1.context-path: /api/v1
  v2.context-path: /api/v2